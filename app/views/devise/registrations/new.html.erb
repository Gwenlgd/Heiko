<h2>Sign up</h2>

<%= simple_form_for resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_name,
                required: true,
                autofocus: true,
                placeholder: "first name?",
                label: false,
                input_html: { autocomplete: "first name" }%>
    <%= f.input :last_name,
                required: true,
                autofocus: true,
                label: false,
                placeholder: "last name?",
                input_html: { autocomplete: "last name" }%>
    <%= f.input :age,
                required: true,
                autofocus: true,
                label: false,
                placeholder: "age?",
                input_html: { autocomplete: "age" }%>
    <%= f.input :email,
                required: true,
                autofocus: true,
                label: false,
                placeholder: "email?",
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                required: true,
                label: false,
                placeholder: "password?",
                input_html: { autocomplete: "password" } %>
    <%= f.input :password_confirmation,
                required: true,
                label: false,
                placeholder: "password?",
                input_html: { autocomplete: "password confirmation" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up", class:"btn-primary" %>
  </div>
<% end %>
    <!--<%= link_to "Log in", new_session_path(resource_name), class: "btn-secondary" %>╌>
